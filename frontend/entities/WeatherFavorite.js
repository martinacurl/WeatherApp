//entity for our weather, only exemple attributes for now
export default class WeatherFavorite {
  constructor(city, latitude, longitude) {
    this.city = city;
    this.latitude = latitude;
    this.longitude = longitude;
  }
}
