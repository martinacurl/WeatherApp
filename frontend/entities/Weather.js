//entity for our weather, only exemple attributes for now
export default class Weather {
  constructor(country, city, weather) {
    this.country = country;
    this.city = city;
    this.weather = weather;
  }
}
